# Nova Microkernel Assignments

Only the assignment-specific implementations are included; the full kernel framework, build system, and infrastructure were provided

For those files, the file structure is as follows:
- `/kern/src/ec_syscall.cc`
- `/user/mem_alloc.c`